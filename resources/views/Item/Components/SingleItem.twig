{% import "Ceres::Item.Macros.AgeRestriction" as AgeRestriction %}

{% set itemData = config("Ceres.item.data") | split(', ') %}

{% set lang = services.sessionStorage.getLang() %}

<script type="x/template" id="vue-single-item">
    <div>
        <slot :getDataField="getDataField" :getFilteredDataField="getFilteredDataField">
            <div class="single container-max page-content">
                <div class="row mt-5 position-relative">
                    <div class="col-12 col-md-7" id="item-container">
                        <slot name="image-carousel"></slot>
                    </div>
                    <div class="col-12 col-md-5 single-rightside">
                        <div {% if config("Legend.singleItemStickBlock.disable") == "false" %}v-stick-in-parent data-sticky-container=".single-rightside"{% endif %}>
                            {% include "PrestijeLegend::Item.Components.SingleItem.SingleItem_Details" %}
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 my-5">
                        {% include "Legend::Item.Components.SingleItem.SingleItem_InformationTable" %}
                    </div>
                </div>
                {% if config("Legend.singleItemParallax.hide") == "false" %}
                <div class="single-item-parallax">
                    <div class="row">
                        <div class="col-12 text-center">
                            {% if lang in ["de", "it", "es", "pl", "fr"] %}
                                {{ config("Legend.singleItemParallax.html_"~lang) | raw }}
                            {% else %}
                                {{ config("Legend.singleItemParallax.html_en") | raw }}
                            {% endif %}
                        </div>
                    </div>
                </div>
                {% endif %}
                <div class="sinle-item-list-container">
                    <slot name="cross-selling-accessory"></slot>
                    <slot name="item-list-container"></slot>
                </div>
                <slot name="feedback-container"></slot>
            </div>
        </slot>
    </div>
</script>
